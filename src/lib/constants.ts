// Theme key is also read by the is:inline FOUC guard in BaseHead.astro,
// which cannot import modules — keep the literal there in sync.
export const THEME_STORAGE_KEY = 'kinetic-theme';
export const MOMENT_IDX_STORAGE_KEY = 'kinetic-moment-idx';
export const CONTACT_ENDPOINT = '/api/contact';
